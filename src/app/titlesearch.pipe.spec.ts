import { TitlesearchPipe } from './titlesearch.pipe';

describe('TitlesearchPipe', () => {
  it('create an instance', () => {
    const pipe = new TitlesearchPipe();
    expect(pipe).toBeTruthy();
  });
});
